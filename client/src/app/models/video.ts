export interface Video {
    title ?: string,
    image ?: string,
    genre ?: string,
    director ?: string,
    status ?: string,
    star ?: string,
    length ?: number,
    description ?: string
}